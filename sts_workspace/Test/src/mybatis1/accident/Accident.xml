<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.oracle.dao.AccidentDao">
	
	<insert id="saveAccident" parameterType="Accident">
		insert into tb_accidenthandle values(null,#{title},#{time},#{source},#{accidentType},#{grade},#{address},#{responseGrade},#{sendTime},#{casualty},#{economicLoss},#{reason},#{picAddress},#{units},#{addressRemark})
	</insert>
</mapper>